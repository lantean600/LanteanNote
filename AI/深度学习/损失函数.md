---
layout: default
title: "有理函数的积分"
mathjax: true
---

## cross_entropy_error
用于计算神经网络分类问题的损失值，衡量模型预测概率分布与真实标签之间的差异。同时用于计算梯度，指出参数变化方向。

```py
def cross_entropy_error(y, t):
    if y.ndim == 1:
        t = t.reshape(1, t.size)
        y = y.reshape(1, y.size)

    if t.size == y.size:
        t = t.argmax(axis=1)

    batch_size = y.shape[0]
    return -np.sum(np.log(y[np.arange(batch_size), t] + 1e-7)) / batch_size

```
